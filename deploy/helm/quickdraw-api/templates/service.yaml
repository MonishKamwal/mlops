apiVersion: v1
kind: Service
metadata:
  name: {{ include "quickdraw-api.fullname" . }}
  labels:
    {{- include "quickdraw-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "quickdraw-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
